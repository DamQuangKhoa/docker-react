language: generic 

sudo: required

services: 
    - docker

before_install:
    - docker build -t ted/docker-react -f Dockerfile.dev . 

script:
    - docker run -e CI=true ted/docker-react npm run test


